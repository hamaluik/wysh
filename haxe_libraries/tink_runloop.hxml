# @install: lix --silent download "haxelib:tink_runloop#0.1.0" into tink_runloop/0.1.0/haxelib
-D tink_runloop=0.1.0
-cp ${HAXESHIM_LIBCACHE}/tink_runloop/0.1.0/haxelib/src
--macro tink.runloop.Boot.boot()
-lib tink_concurrent
-lib tink_syntaxhub